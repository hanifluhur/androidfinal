<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hanifluhur.myapplication">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_HISTORY_BOOKMARKS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RegisterActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".Main3Activity" />
        <activity android:name=".Main4Activity" />
        <activity android:name=".MainActivityMerapi" />
        <activity android:name=".MainActivityApi" />
        <activity android:name=".MainActivityKalibiru" />
        <activity android:name=".MainActivityParangtritis" />
        <activity android:name=".MainActivityIndrayanti" />
        <activity android:name=".MainActivityGlagah" />
        <activity android:name=".MainActivityBorobudur" />
        <activity android:name=".MainActivityPrambanan" />
        <activity android:name=".MainActivityRatu" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsBorobudur"
            android:label="@string/title_activity_maps_borobudur" />
        <activity
            android:name=".MapsPrambanan"
            android:label="title_activity_maps_prambanan" />
        <activity
            android:name=".MapsRatu"
            android:label="title_activity_maps_ratu" />
        <activity
            android:name=".MapsMerapi"
            android:label="title_activity_maps_merapi" />
        <activity
            android:name=".MapsApi"
            android:label="title_activity_maps_api" />
        <activity
            android:name=".MapsParangtritis"
            android:label="title_activity_maps_parangtritis" />
        <activity
            android:name=".MapsGlagah"
            android:label="title_activity_maps_glagah" />
        <activity
            android:name=".MapsKalibiru"
            android:label="title_activity_maps_kalibaru" />
        <activity
            android:name=".MapsIndrayanti"
            android:label="title_activity_maps_indrayanti" />
        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps"></activity>
    </application>

</manifest>